# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Database (SQLite for dev, Postgres later)
sqlalchemy==2.0.35
alembic==1.13.3

# Env loading
python-dotenv==1.0.1

# Testing
pytest==8.3.3
httpx==0.27.2

# ML + ingestion packages are added in later phases (Phase 2+)
# to keep this initial install fast.
